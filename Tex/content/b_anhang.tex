\chapter{Berechnung von $U_C$ im Falle leichter Dotierung}\label{sec:anhang_b}

Es soll gezeigt werden, dass $U_C$ für alle $\abs{\mu} < E_D$ in der Näherung $\rho(\epsilon) \approx A \abs{\epsilon}$ verschwindet, für die $\mu \neq 0$ gilt.
Dazu betrachte die Selbstkonsistenzgleichung für beliebige Füllung~\eqref{eq:gap_eq_arbitrary_filling_integral} bei $T= 0$ und für $\Delta \neq 0$:
\begin{align}
    1 = \frac{UA}{2} \int_{\mu - E_D}^{\mu + E_D} \frac{\abs{\epsilon}}{\sqrt{(\epsilon - \mu)^2 + \Delta_0^2}} \, d\epsilon = \frac{UA}{2} \int_{-E_D}^{E_D} \frac{\abs{\xi + \mu}}{\sqrt{\xi^2 + \Delta_0^2}} \,  d\xi
\end{align}
mit $\xi = \epsilon - \mu$.
Es gilt also 
\begin{align}
    U(\Delta_0) = \frac{2}{A\cdot F(\Delta_0)}
\end{align}
mit 
\begin{align}
    F(\Delta_0) &= \int_{-E_D}^{E_D} \frac{\abs{\xi + \mu}}{\sqrt{\xi^2 + \Delta_0^2}} \,  d\xi \, .
\end{align}

Das Integral wird per Fallunterscheidung unter der Annahme $\abs{\mu} < E_D$ aufgeteilt:
\begin{align}
    F(\Delta_0) &= -\int_{-E_D}^{-\mu} \frac{\xi + \mu}{\sqrt{\xi^2 + \Delta_0^2}} \, d\xi + \int_{-\mu}^{E_D} \frac{\xi + \mu}{\sqrt{\xi^2 + \Delta_0^2}}\, d\xi = g(-E_D) + g(E_D) \, ,
\end{align}
und mit 
\begin{align}
    g(x) = \int_{-\mu}^{x} \frac{\xi + \mu}{\sqrt{\xi^2 + \Delta_0^2}}\, d\xi = \sqrt{x^2 + \Delta_0^2} - \sqrt{\mu^2 + \Delta_0^2} + \mu \left(\operatorname{arsinh}\left(\frac{x}{\Delta_0}\right) - \operatorname{arsinh}\left(\frac{-\mu}{\Delta_0}\right)\right) \, ,
\end{align}
ergibt sich 
\begin{align}
    F(\Delta_0) &= 2\sqrt{E_D^2 + \Delta_0^2} - \sqrt{\mu^2 + \Delta_0^2} + \mu \left[\operatorname{arsinh}\left(\frac{E_D}{\Delta_0}\right) + \operatorname{arsinh}\left(\frac{-E_D}{\Delta_0}\right) - 2 \operatorname{arsinh}\left(\frac{-\mu}{\Delta_0}\right)  \right] \, .
\end{align}
Da die kritische Kopplung $U_C$ über den Limes $\Delta_0 \rightarrow 0$ von $U$ definiert ist, verschwindet $U_C$ genau dann, wenn $F(\Delta_0)$ divergiert. 
Die Wurzelterme in $F(\Delta_0)$ konvergieren einfach gegen eine Konstante, weswegen also nur noch die Konvergenz des $\arsinh$-Terms überprüft werden muss.

Aufgrund der Punktsymmetrie von $\arsinh$ kann dieser jedoch stark vereinfacht werden, da somit $\arsinh(E_D/\Delta_0) + \arsinh(-E_D/\Delta_0) = 0$ gilt und nur $-2\mu \arsinh(-\mu/\Delta_0)$ übrig bleibt.
Für $\Delta_0 \rightarrow 0$ divergiert das Argument und der übrigbleibende $\arsinh$ divergiert daher ebenfalls logarithmisch.
Für $\mu = 0$ entfällt diese logarithmische Divergenz und $F(\Delta_0) \rightarrow F(0) = 2E_D$, man erhält also das Ergebnis aus~\eqref{eq:U_C}.
Damit ist gezeigt, dass die kritische Kopplung, außer bei $\mu = 0$ verschwindet.


% Tatsächlich divergiert jeder der $\arsinh$-Terme für sich aufgrund des $\Delta_0$ im Nenner des Arguments. 
% Aufaddiert bzw.\ subtrahiert könnten sich diese Divergenzen allerdings aufheben, weswegen der $\arsinh$-Term nun genauer untersucht wird. 
% Für große Argumente $x>0$ gilt $\arsinh(x) \rightarrow \ln(2x)$, und für $x<0$ $\arsinh(x) \rightarrow -\ln(2\abs{x})$.
% Damit wird der $\arsinh$-Term, über die $\ln$-Regeln vereinfacht, im Limes zu 
% \begin{align}
%     \ln\left(\left(\frac{2E_D}{\Delta_0}\right) \cdot \left(\frac{\Delta_0}{2E_D}\right) \cdot \left(\frac{2\mu}{\Delta_0}\right)^2\right) \, .
% \end{align}
% wird.